export const ShowFormData = ({username,email, age})=>{
    return (
        <>
            <div>
                <span>{username}</span>
                <span>{email}</span>
                <span>{age}</span>
            </div>
        </>
    )
}